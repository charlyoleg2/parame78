import { describe, it, expect } from 'vitest';
import * as dut from './index';

describe('sheetfold under tests', () => {
	// angle conversion
	it('a dummy test', () => {
		expect(2+2).toBe(4);
	});
});
